---
title: Building Simple Websites with Astro
description: Learn how to build a simple, modern landing page using Astro, a fast and flexible web framework for content-focused websites.
link: https://github.com/liiam-dsouza/website
tags: [🚀 Astro, 🎨 Tailwind CSS, 🟦 TypeScript]
---
import { FileTree } from "@astrojs/starlight/components"

## Workshop Overview

In this workshop, you’ll build a **simple, modern landing page** using **Astro**, a fast and flexible web framework for content-focused websites.

By the end of the session, you will have:
- Set up a brand-new Astro project from scratch
- Added Tailwind CSS for styling
- Created reusable layouts and components
- Built a clean, responsive landing page
- Gained confidence navigating an Astro codebase

This workshop is hands-on and code-heavy. You’ll be writing real code and seeing changes instantly in the browser.

[PLACE HOLDER FOR FINAL LANDING PAGE PREVIEW]

## Workshop Context

Modern websites don’t have to be complex to be powerful.

Astro is designed to help developers:
- Ship **fast websites** with minimal JavaScript
- Organise UI using **components**
- Scale from simple static pages to larger sites
- Work comfortably with modern tools like Tailwind, Markdown, and TypeScript

This workshop focuses on **foundational web development concepts**:
- Project structure
- Component-based design
- Layout composition
- Styling with utility-first CSS

These skills transfer directly to frameworks like React, Next.js, Svelte, and beyond.

## Learning Objectives

By the end of this workshop, you should be able to:

- Explain what Astro is and when to use it
- Create a new Astro project using a package manager
- Add Tailwind CSS to an Astro project
- Understand Astro’s file and folder structure
- Build and reuse components
- Create a layout and use it across pages
- Assemble a simple landing page using components

## How To: Building a Simple Website with Astro

### 1. Prerequisites

Make sure you have the following installed:

- **Node.js** (LTS recommended)
- A package manager:
  - `npm` (comes with Node)
  - or `pnpm`
  - or `yarn`
- A code editor (VS Code recommended)

[PLACE HOLDER FOR NODE VERSION CHECK TERMINAL]

Check Node is installed:

```bash
node -v
```

### 2. Create a New Astro Project

In your terminal, run:

```bash
npm create astro@latest
```

or with pnpm:

```bash
pnpm create astro@latest
```

or with yarn:

```astro title="src/layouts/AstroWorkshopLayout.astro" showLineNumbers
---
const { title = "Astro Workshop" } = Astro.props;
---
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>{title}</title>
    </head>
    <body class="min-h-screen bg-neutral-50 text-neutral-900">
        <main class="max-w-5xl mx-auto px-6 py-12">
            <slot />
        </main>
    </body>
</html>
```
Password: FishingHarp13%
```
src/
├─ 🧩 components/     → Reusable UI pieces
├─ 🗄️ layouts/        → Page layouts
└─ 📄 pages/          → Routes (URLs)
```
