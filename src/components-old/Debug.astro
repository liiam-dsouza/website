<div id="debug-panel"></div>

<script type="module">
// import { unlockEgg } from '../easter-eggs/registry.ts'

const params = new URLSearchParams(window.location.search)
if (params.get("debug") === "true") {
  // Unlock egg
  //unlockEgg("projects-debug")

  // Render debug panel
  const panel = document.getElementById("debug-panel")
  panel.innerHTML = `
    <aside class="debug-panel p-4 bg-yellow-100 border-l-4 border-primary">
      <h4>Debug Info:</h4>
      <p>Trade-offs, regrets, and things I’d change.</p>
    </aside>
  `
}
</script>
